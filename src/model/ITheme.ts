export interface ITheme {
  theme: {
    colors: {
      grey: string;
      darkGrey: string;
    };
    shadows: {
      normal: string;
      hover: string;
    };
    gradients: {
      primary: string;
    };
  };
}
